services:
  redis-dev:
    image: redis:7-alpine
    container_name: pulseboard-redis-dev
    command: ["redis-server", "--appendonly", "no", "--save", ""]
    ports:
      - "6390:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 5
